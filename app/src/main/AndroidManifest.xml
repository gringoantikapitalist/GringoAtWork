<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tequilagrexit.jde">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/jde1"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/jde1_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PairOfLines" />
        <activity android:name=".jdeFabo" />
        <activity android:name=".FaboActivity" />
        <activity android:name=".ToTheEnd"
                  android:parentActivityName=".MainActivity">
        </activity>
    </application>

</manifest>